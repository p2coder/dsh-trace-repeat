# dsh-trace-repeat bundle patch: one host-only row. lib/index.js records
# per-session traces (reasoning/tool completions as atomic immutable versions),
# syncs versions to a dedicated git repo with worktree materialization, and
# registers the /trace command family for timeline replay and resume.
- insert:
    - id: trace-repeat
      name: 'dsh-trace-repeat'
